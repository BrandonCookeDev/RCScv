{
  "name": "react-server",
  "version": "1.0.0",
  "description": "",
  "main": "express-server.js",
  "scripts": {
    "install-all": "npm install && cd webapp && npm install",
    "start": "npm run watch | npm run launch",
    "launch": "node express-server.js",
    "build": "cd webapp && npm run build",
    "watch": "cd webapp && npm run watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.18.2",
    "compression": "^1.7.2",
    "dotenv": "^5.0.1",
    "express": "^4.16.3",
    "lodash": "^4.17.10",
    "winston": "^2.4.2"
  }
}
